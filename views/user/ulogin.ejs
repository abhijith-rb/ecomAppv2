<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Form</title>
    <script
      src="https://kit.fontawesome.com/66aa7c98b3.js"
      crossorigin="anonymous"
    ></script>
    <!-- <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"> -->
  </head>
  <body>
    <style>
        *{
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      .container {
        height: 100vh;
        margin: 0 auto;
        position: relative;
        /* background: linear-gradient(to right,  #FFD333, #FF7F00, #e5b509, #FF7F00); */
        background-color: #FFD333;
      }

      .container .form-1 {
        display: flex;
        flex-direction: column;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background: #fff;
        width: 40%;
        box-shadow: 0 19px 38px rgba(0, 0, 0, 0.3);
      }

      .form-1 h1 {
        color: #6c757d;
        text-align: center;
        margin-top: 0.7rem;
        margin-bottom: 1.5rem;
      }

      input[type="text"],
      input[type="password"] {
        border: none;
        outline: none;
        border-bottom: 1px solid;
        background: none;
        margin: 0.9rem 2rem;
        font-size: 1rem;
      }

      label {
        margin: 0 2rem;
      }

      span {
        margin: 0 2rem;
        color: blue;
        cursor: pointer;
      }

      button {
        margin-top: 2rem;
        margin-bottom: 1.5rem;
        margin-left: 5rem;
        margin-right: 5rem;
        padding: 0.5rem;
        cursor: pointer;
        border-radius: 1rem;
        border: none;
        font-size: 1.1rem;
        font-weight: bolder;
        color: #6c757d;
        /* background: linear-gradient(to right, #FFD333, #FF7F00, #e5b509, #FF7F00); */
        background-color: #FFD333;
      }

      .lbt {
        margin-top: 1rem;
        margin-bottom: 1rem;
        margin-left: 25%;
        margin-right: 25%;
        padding-top: 0.5rem;
        padding-bottom: 0.5rem;
        text-align: center;
        cursor: pointer;
        border-radius: 1rem;
        border: none;
        font-size: 1.1rem;
        font-weight: bolder;
        color: #6c757d;
        /* background: linear-gradient(to right, #FFD333, #FF7F00, #e5b509, #FF7F00); */
        background-color: #FFD333;
      }

/* ........///Sign-Up///......... */

      p {
        text-align: center;
        font-weight: bolder;
      }

      .icons {
        display: flex;
        justify-content: center;
        margin-bottom: 3rem;
        margin-top: 0.5rem;
      }

      .icons a {
        text-decoration: none;
        font-size: 1rem;
        margin: 0.2rem;
      }

      .icons .fa-facebook-f {
        border-radius: 50%;
        background: #5d75ab;
        color: #fff;
        padding: 1rem;
      }

      .icons .fa-twitter {
        border-radius: 50%;
        background: #1da1f2;
        color: white;
        padding: 1rem;
      }

      .icons .fa-google {
        border-radius: 50%;
        background: #ee5645;
        color: #fff;
        padding: 1rem;
      }

/* ....///Media query///..... */

      @media (max-width: 501px) {
        html {
          font-size: 15px;
        }

        .container .form-1 {
          width: 300px;
        }
      }

      @media (min-width: 380px) and (max-width: 501px) {
        html {
          font-size: 15px;
        }

        .container .form-1 {
          margin-top: -100px;
          width: 300px;
        }
      }

      @media (min-width: 501px) and (max-width: 768px) {
        html {
          font-size: 14px;
        }

        .container .form-1 {
          width: 450px;
        }
      }

      @media (min-width: 765px) and (max-width: 1200px) {
        html {
          font-size: 18px;
        }

        .container .form-1 {
          margin-top: -220px;
          width: 540px;
          height: 550px;
        }
      }

      @media (orientation: landscape) and (max-height: 500px) {
        .container {
          height: 100vmax;
        }
      }
    </style>
    <div class="container">
      <form class="form-1" action="/login" method="post" id="loginForm">
        <h1>Login</h1>
        <label for="email">Username</label>
        <input type="text" name="username" id="uname" />
        <label for="password">Password</label>
        <input type="password" name="password" id="password"  />
        <button type="submit">Login</button>

        <p style="color:red" id="error-message"></p>

        <a class="lbt" href="/otpLogin" style="text-decoration: none;">
          OTP Login
      </a>
  
      <a href="/register" class="lbt" style="text-decoration: none;">Create an Account</a>
  
      <a class="lbt" href="/admin/adminLogin" style="text-decoration: none;">
          Admin Login
      </a>
      </form>
    </div>

    

    <script>

        const form = document.getElementById("loginForm");

        form.addEventListener('submit', async (event) => {
            event.preventDefault();

            const username = document.getElementsByName("username")[0].value;
            const password = document.getElementsByName("password")[0].value;

            const usernameRegex = /^[a-zA-Z0-9]{3,20}$/ ;
            const passwordRegex = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)[a-zA-Z\d@$!%*?&]{8,20}$/

            if (!usernameRegex.test(username)) {
                alert("Username must be at least 3 characters long and valid");
                return;
            }
            
            if (!passwordRegex.test(password)) {
                alert("Password not valid");
                return;
            }

            const response = await fetch('/login', {
                method: 'POST',
                headers: {
                'Content-Type': 'application/json'
                },
                body: JSON.stringify({ username, password })
            });

            const data = await response.json();
            if (response.ok) {
                window.location.href = '/';
            } else {
                const errorElement = document.getElementById('error-message');
                errorElement.textContent = data;
            }
    });

           
    </script>

  </body>
</html>